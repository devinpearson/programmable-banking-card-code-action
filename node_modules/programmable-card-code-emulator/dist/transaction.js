import {} from './merchant.js';
import { merchantCategoryFromCode } from './merchant-category.js';
import { countryFromString } from './country.js';
import { currencyFromString } from './currency.js';
export const createTransaction = (currencyCode, centsAmount, merchantCode, merchantName, merchantCity, countryCode) => {
    let date = new Date();
    let dateString = date.toISOString();
    const country = countryFromString(countryCode.toUpperCase());
    const category = merchantCategoryFromCode(merchantCode);
    const cc = currencyFromString(currencyCode.toUpperCase());
    return {
        accountNumber: '10000000000',
        dateTime: dateString,
        centsAmount: centsAmount,
        currencyCode: cc.toLowerCase(),
        type: 'card',
        reference: 'simulation',
        card: {
            id: '2280000',
        },
        merchant: {
            category: category,
            name: merchantName,
            city: merchantCity,
            country: country,
        },
    };
};
//# sourceMappingURL=transaction.js.map