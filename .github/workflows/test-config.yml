name: Test YAML Config Action
on:
  workflow_dispatch:
jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run programmable-banking-card-code-action
        uses: ./
        with:
          who-to-greet: "Test User"
          config-file: example-config.yml
